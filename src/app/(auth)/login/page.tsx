import { AccessCodeForm } from "@/components/auth/access-code-form";

export const metadata = { title: "Login — Healing Jesus Conference" };

export default function LoginPage() {
  return <AccessCodeForm />;
}
